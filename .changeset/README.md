# Changesets

This project uses [changesets](https://github.com/changesets/changesets) for version management and changelog generation.

When making changes to the `bones` package, add a changeset:

```sh
vp dlx changeset
```

This will prompt you to select the package, bump type (patch/minor/major), and a summary of the change.
